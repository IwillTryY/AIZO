// Package policy provides a rule-based policy engine for AIZO.
// Policies are defined in YAML files and evaluated against actions
// before execution. Supports allow/deny rules, actor/resource matching,
// glob patterns, and per-action rate limiting.
package policy
