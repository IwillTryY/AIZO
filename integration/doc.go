// Package integration provides bridges between AIZO layers.
// The Bridge connects Layer 1 (adapters) with Layer 2 (entity catalog),
// enabling adapter health to flow into entity state and commands to
// be routed through the appropriate adapter for each entity.
package integration
