// Package layer2 provides entity discovery and registration for AIZO.
// It maintains a catalog of all managed entities (servers, APIs, databases,
// containers, devices) and maps relationships between them.
// Entities are discovered automatically via network/cloud scanning or
// registered manually through the RegistrationAPI.
package layer2
